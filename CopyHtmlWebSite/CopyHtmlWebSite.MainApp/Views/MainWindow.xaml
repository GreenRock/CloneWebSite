<Window x:Class="CopyHtmlWebSite.MainApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:mainApp="clr-namespace:CopyHtmlWebSite.MainApp"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525" MinHeight="350" MaxHeight="700" MinWidth="525" MaxWidth="1000">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" prism:RegionManager.RegionName="{ x:Static mainApp:Regions.MainRegion }" HorizontalAlignment="Stretch">

        </ContentControl>
        <ContentControl Grid.Column="1" prism:RegionManager.RegionName="{ x:Static mainApp:Regions.RightRegion }" HorizontalAlignment="Stretch">
            
        </ContentControl>

    </Grid>

</Window>
